{
  "name": "e3-admin-mobile",
  "version": "1.0.0",
  "description": "E3 Admin Mobile App",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "cap:add": "npx cap add",
    "cap:sync": "npx cap sync",
    "cap:build": "npm run build && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.3.0"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
