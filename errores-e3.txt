ERRORES A RESOLVER EN PROYECTO E3 - sleepy-saturn
====================================================

1. IMPORTS NO UTILIZADOS (index.astro:11-12)
   - Eliminar: import { getComponente } from '../data/componentes';
   - Eliminar: import type { CarouselItem } from '../data/carouselItems';

2. DUPLICACION DE DATOS    ====>  opcion a ====> priorizar un tiempo de espera de 5 segundos para esperar respuesta de api para los elementos y carrousell
   - Opcion A: Usar featureItems de carouselItems.ts y eliminar defaultItems en index.astro
   - Opcion B: Mantener defaultItems y eliminar featureItems de carouselItems.ts (si no se usa en otro lado)

3. FUNCION getComponente NO SE USA (componentes.ts:24-26) ====> usa la opcion a
   - Opcion A: Eliminar la funcion si no se necesita 
   - Opcion B: Usarla en index.astro en vez del fetch directo

4. FALLBACK INCONSISTENTE EN API URL
   - index.astro:16 usa 'https://e3-admin-api.onrender.com'
   - componentes.ts:10 usa 'http://localhost:5000'====> eliminar local solo usar api en produccion despues hacer una funcion que mande datos en local
   - Unificar a una sola constante o variable de entorno

5. VALIDACION DEBIL DE DATOS DE API====> poner en pendientes
   - Validar estructura completa de los datos (id, title, description, image, contact)
   - Agregar validacion de campos requeridos antes de asignar

6. ERROR TIPOGRAFICO (Layout.astro:9)
   - Cambiar 'E^3' por 'E3' o 'E³'====> cambia a E3

7. VARIABLES NO USADAS EN INTERFAZ (componentes.ts)
   - La interfaz Componente define subtitulo, link, extra_data pero nunca se usan ====> lee el codigo y dame un flujo ya decidire despues de leer el flujo
   - Considerar simplificar la interfaz o documentar por que existen ====> documenta todo como se conecta y como sirven en un archivo por separado haz un archivo con las dependencias para el futuro

8. CACHE HEADERS SILENCIOSOS (index.astro:20-22)
   - El try/catch vacio oculta errores de configuracion ====> dame opciones
   - Agregar logging o console.warn para debugging ====> ok

ACCIONES SUGERIDAS:
1. Hacer fetch de los datos necesarios con validacion estricta
2. Unificar fuentes de datos (evitar duplicacion)
3. Limpiar imports no utilizados
4. Corregir errores tipograficos y de consistencia
